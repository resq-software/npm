name: Post Release

on:
  release:
    types:
      - published

permissions:
  issues: write
  pull-requests: write

jobs:
  post_release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false
      - run: echo "npm_version=$(npm pkg get version | tr -d '"')" >> "$GITHUB_ENV"
        working-directory: packages/ui
      - uses: apexskier/github-release-commenter@e7813a9625eabd79a875b4bc4046cfcae377ab34 # v1.4.1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          comment-template: |
            :tada: This is included in version {release_link} :tada:

            The release is available on:

            * [GitHub releases](https://github.com/resq-software/npm/releases/tag/{release_tag})
            * [npm package (@latest dist-tag)](https://www.npmjs.com/package/@resq-systems/ui/v/${{ env.npm_version }})

            Cheers! 📦🚀
