name: Release

on:
  push:
    branches:
      - master

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

permissions:
  contents: write
  id-token: write
  packages: write
  pull-requests: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          ref: master
          # zizmor: ignore[artipacked] -- changesets/action needs credentials to push version bumps
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up git user
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"

      - uses: ./.github/actions/prepare

      - name: Set up npm auth
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > ~/.npmrc

      - name: Run Changesets
        id: changesets
        uses: changesets/action@a45c4d594aa4e2c509dc14a9f2b3b67ba3780d0d # v1.9.0
        with:
          title: "[Release] Version Packages"
          commit: "chore: version packages"
          publish: bun run release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @resq-systems/ui to GitHub Packages
        uses: ./.github/actions/publish-github-packages
        with:
          package-dir: packages/ui
          github-token: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Publish @resq-systems/dsa to GitHub Packages
        uses: ./.github/actions/publish-github-packages
        with:
          package-dir: packages/dsa
          github-token: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Publish @resq-systems/helpers to GitHub Packages
        uses: ./.github/actions/publish-github-packages
        with:
          package-dir: packages/helpers
          github-token: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Publish @resq-systems/http to GitHub Packages
        uses: ./.github/actions/publish-github-packages
        with:
          package-dir: packages/http
          github-token: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Publish @resq-systems/logger to GitHub Packages
        uses: ./.github/actions/publish-github-packages
        with:
          package-dir: packages/logger
          github-token: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Publish @resq-systems/decorators to GitHub Packages
        uses: ./.github/actions/publish-github-packages
        with:
          package-dir: packages/decorators
          github-token: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Publish @resq-systems/rate-limiting to GitHub Packages
        uses: ./.github/actions/publish-github-packages
        with:
          package-dir: packages/rate-limiting
          github-token: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Publish @resq-systems/security to GitHub Packages
        uses: ./.github/actions/publish-github-packages
        with:
          package-dir: packages/security
          github-token: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true
