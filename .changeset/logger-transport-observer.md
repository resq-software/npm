---
"@resq-systems/logger": minor
---

Implement the LogTransport Observer pipeline: Logger.addTransport/removeTransport/clearTransports fan every log out as a structured LogEntry with full error isolation, plus built-in MemoryTransport, JsonTransport, and composable createFilterTransport/byLevel
