---
"@resq-systems/ui": minor
---

Add a `stale` prop to every reading instrument plus `isStale`/`readingAge`/`latestTimestamp` adapters, and rank the ContactScope render cap by CPA risk instead of range
