# Build outputs (per-package, not src/lib/)
**/lib/
!**/src/lib/
storybook-static/
.next/
# `email export --outDir ./out` render output (email-templates)
**/out/

# Test artifacts
coverage/

# Dependencies
node_modules/

# Lock files from other package managers (this project uses bun)
yarn.lock
package-lock.json
pnpm-lock.yaml

# Environment
.env
.env.*
!.env.example

# Editor — VS Code (keep checked-in configs)
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json
!.vscode/tasks.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log
pnpm-debug.log*

# Tooling caches
.turbo/
.eslintcache
.prettiercache
*.tsbuildinfo
node-compile-cache/

# Internal working doc (not published with the package)
packages/email-templates/EMAIL_CONTENT_AND_LEGAL_GUIDE.md

# TypeDoc
docs/

# Claude / Serena
.claude/*
!.claude/skills/
.serena/cache/

# Local worktrees
.worktrees/

# Generated by `bun run a11y` — the audit artefact, not a source file.
a11y-report.json
